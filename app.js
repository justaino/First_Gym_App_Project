/*
  app.js — the app logic for the Gym App.

  Phase 0: there is nothing to do yet! This file is wired into index.html
  so it's ready for Phase 1 (profiles + exercises). For now it just logs a
  message so we can confirm the script loaded correctly.

  How to check it's connected:
    1. Open the page in the browser (see README / Live Server steps).
    2. Open the browser's developer tools (right-click → Inspect → Console).
    3. You should see the message below printed in the Console.
*/

console.log("Gym App loaded — Phase 0 setup is working ✅");
